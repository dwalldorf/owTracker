parameters:
    cronjobs:
        processUserScores:
            command: owt:processUserScores
            interval: 10
        readDemoInfo:
            command: rabbitmq:consumer demo_info_upload -m 50
            interval: 2